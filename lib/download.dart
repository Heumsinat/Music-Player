import 'package:flutter/material.dart';

class Download extends StatelessWidget {
  // final int index;
  // Download(this.index);
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text('Download songs'),
      ),
    );
  }
}